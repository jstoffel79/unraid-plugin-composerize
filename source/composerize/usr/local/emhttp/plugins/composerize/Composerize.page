---
Title="Composerize"
Icon="composerize.png"
Type="xmenu"
Menu="Utilities"
---
<?php
// --- Final Debugging Step ---
ini_set('display_errors', '1');
error_reporting(E_ALL);

echo "<h1>Composerize Plugin Debugging</h1>";

try {
    echo "<p style='color: lightgreen;'><strong>Step 1:</strong> PHP script started successfully.</p>";
    
    require_once __DIR__ . '/include/composerize.php';
    
    echo "<p style='color: lightgreen;'><strong>Step 2:</strong> Helper file 'composerize.php' included successfully.</p>";
    
    echo "<p style='color: yellow;'><strong>Step 3:</strong> Attempting to run getDockerTemplateList()...</p>";
    
    // Test the function that is the most likely point of failure.
    $templateList = getDockerTemplateList();
    
    echo "<p style='color: lightgreen;'><strong>Step 4: SUCCESS!</strong> The getDockerTemplateList() function executed without errors.</p>";
    echo "<p>Found " . count($templateList) . " templates.</p>";
    echo "<p>The blank page issue is likely caused by something else in the HTML/JS portion. The PHP backend is working.</p>";

} catch (Throwable $t) {
    // If we get here, a fatal error occurred.
    echo "<p style='color: red;'><strong>Step 4: FAILURE!</strong> A fatal error occurred.</p>";
    echo "<p><strong>Error Message:</strong> " . $t->getMessage() . "</p>";
    echo "<p><strong>File:</strong> " . $t->getFile() . " on line " . $t->getLine() . "</p>";
    echo "<h3>Stack Trace:</h3><pre>" . $t->getTraceAsString() . "</pre>";
    exit;
}

// If the script reaches this point, the PHP is likely fine.
// We will render the rest of the page.
define('PLUGIN_ROOT', '/plugins/composerize');
define('PLUGIN_API_URL', PLUGIN_ROOT . '/api.php');
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Composerize</title>
    <style>
        :root {
            --primary-bg: #2d3748;
            --secondary-bg: #4a5568;
            --border-color: #718096;
            --text-color: #e2e8f0;
            --accent-color: #38a169;
        }
        body {
            font-family: sans-serif;
            background-color: var(--primary-bg);
            color: var(--text-color);
            margin: 0;
            padding: 1.5rem;
        }
        .container {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
            max-width: 1200px;
            margin: 0 auto;
        }
        .panel {
            background-color: var(--secondary-bg);
            border-radius: 0.5rem;
            padding: 1.5rem;
        }
        h2 {
            margin-top: 0;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 0.5rem;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="panel">
        <h2>Select a Template</h2>
        <select id="template-select">
            <option value="" selected>Choose a template...</option>
            <?php foreach ($templateList as $name => $xmlPath): ?>
                <option value="<?= htmlspecialchars($xmlPath); ?>"><?= htmlspecialchars($name); ?></option>
            <?php endforeach; ?>
        </select>
    </div>
</div>
</body>
</html>
