---
Title="Composerize"
Icon="composerize.png"
Type="xmenu"
Menu="Utilities"
---
<?php
// --- Advanced Debugging ---
// This script will test each step of the loading process to find the exact point of failure.
ini_set('display_errors', '1');
error_reporting(E_ALL);

echo "<h1>Composerize Plugin Debugging</h1>";

try {
    echo "<p style='color: lightgreen;'><strong>Step 1:</strong> PHP script started successfully.</p>";
    
    // Test if the main helper file can be included.
    require_once '/usr/local/emhttp/plugins/composerize/include/composerize.php';
    
    echo "<p style='color: lightgreen;'><strong>Step 2:</strong> Helper file 'composerize.php' was included without a fatal error.</p>";
    
} catch (Throwable $t) {
    // This block will catch an error if 'composerize.php' or one of ITS dependencies fails to load.
    echo "<p style='color: red;'><strong>FAILURE at Step 2:</strong> Could not include 'composerize.php'. This is likely due to an error within that file or one of its dependencies (DockerClient.php or Helpers.php).</p>";
    echo "<p><strong>Error Message:</strong> " . $t->getMessage() . "</p>";
    echo "<p><strong>File:</strong> " . $t->getFile() . " on line " . $t->getLine() . "</p>";
    echo "<h3>Stack Trace:</h3><pre>" . $t->getTraceAsString() . "</pre>";
    exit;
}

try {
    echo "<p style='color: yellow;'><strong>Step 3:</strong> Attempting to call getDockerTemplateList()...</p>";
    
    // Test the first function that relies on the included files.
    $templateList = getDockerTemplateList();
    
    echo "<p style='color: lightgreen;'><strong>Step 4: SUCCESS!</strong> The getDockerTemplateList() function executed correctly.</p>";
    echo "<p>Found " . count($templateList) . " templates.</p>";
    echo "<p>If you see this message, the PHP backend is working correctly. The issue may be in the HTML or JavaScript portion of the file.</p>";

} catch (Throwable $t) {
    // This block will catch an error if the function itself fails.
    echo "<p style='color: red;'><strong>FAILURE at Step 3:</strong> The function getDockerTemplateList() caused a fatal error.</p>";
    echo "<p><strong>Error Message:</strong> " . $t->getMessage() . "</p>";
    echo "<p><strong>File:</strong> " . $t->getFile() . " on line " . $t->getLine() . "</p>";
    echo "<h3>Stack Trace:</h3><pre>" . $t->getTraceAsString() . "</pre>";
    exit;
}

// If the script reaches this point, the PHP is likely fine.
define('PLUGIN_ROOT', '/plugins/composerize');
define('PLUGIN_API_URL', PLUGIN_ROOT . '/api.php');
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Composerize</title>
    <style>
        :root {
            --primary-bg: #2d3748;
            --secondary-bg: #4a5568;
            --border-color: #718096;
            --text-color: #e2e8f0;
            --accent-color: #38a169;
        }
        body {
            font-family: sans-serif;
            background-color: var(--primary-bg);
            color: var(--text-color);
            margin: 0;
            padding: 1.5rem;
        }
        .container {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
            max-width: 1200px;
            margin: 0 auto;
        }
        .panel {
            background-color: var(--secondary-bg);
            border-radius: 0.5rem;
            padding: 1.5rem;
        }
        h2 {
            margin-top: 0;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 0.5rem;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="panel">
        <h2>Select a Template</h2>
        <select id="template-select">
            <option value="" selected>Choose a template...</option>
            <?php foreach ($templateList as $name => $xmlPath): ?>
                <option value="<?= htmlspecialchars($xmlPath); ?>"><?= htmlspecialchars($name); ?></option>
            <?php endforeach; ?>
        </select>
    </div>
</div>
</body>
</html>
